---
/**
 * Unified dynamic route for:
 * - 4 pillar pages (from content/pillars collection)
 * - 3 local landing pages (from data/locals.js)
 *
 * Static routes like /hakkimizda, /sss, /tesekkurler take priority over this.
 */
import { getCollection, render } from 'astro:content';
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.jsx';
import Footer from '../sections/Footer.jsx';
import StickyContactBar from '../components/StickyContactBar.jsx';
import ConsentNotice from '../components/ConsentNotice.jsx';
import WhatsAppButton from '../components/WhatsAppButton.jsx';
import { localLandings } from '../data/locals';
import { services } from '../data/services';
import { commonProblems } from '../data/common-problems';
import { crossPages } from '../data/cross-pages';
import { neighborhoods } from '../data/neighborhoods';
import { districtProfiles } from '../data/district-profiles';
import { districtLocalNotes } from '../data/district-local-notes';
import { articleSchema, breadcrumbSchema, localBusinessSchema, faqSchema, serviceSchema } from '../lib/schema';

// Deterministik hash — slug'dan sayı üretir (her lokasyon için farklı, build-stable)
function slugHash(s: string): number {
  let h = 5381;
  for (let i = 0; i < s.length; i++) h = ((h << 5) + h + s.charCodeAt(i)) | 0;
  return Math.abs(h);
}
// Slug prefix → districtProfiles key eşleme
function districtKeyForSlug(slug: string): string | null {
  const prefix = slug
    .replace(/-it-destegi$/, '')
    .replace(/-merkez$/, '')
    .replace(/-finans-merkezi$/, '')
    .replace(/-sanayi$/, '')
    .split('-')[0];
  return districtProfiles[prefix] ? prefix : null;
}

export async function getStaticPaths() {
  const PILLAR_META: Record<number, { slug: string; title: string }> = {
    1: { slug: 'yonetilen-it-hizmetleri', title: 'Yönetilen IT Hizmetleri' },
    2: { slug: 'kobi-siber-guvenlik', title: 'KOBİ Siber Güvenlik' },
    3: { slug: 'sirket-veri-yedekleme', title: 'Şirket Veri Yedekleme' },
    4: { slug: 'kurumsal-network-kurulumu', title: 'Kurumsal Network Kurulumu' },
  };

  const pillars = await getCollection('pillars');
  const allPosts = await getCollection('blog');

  const pillarPaths = pillars.map(p => {
    const pillarSlug = p.data.slug || p.id;
    const meta = PILLAR_META[p.data.pillar as number] || { slug: pillarSlug, title: p.data.title };
    const clusters = allPosts.filter(bp => bp.data.pillar === p.data.pillar);
    return {
      params: { slug: pillarSlug },
      props: { type: 'pillar', entry: p, meta, clusters, local: null, cross: null, hood: null },
    };
  });

  const localPaths = localLandings.map(l => ({
    params: { slug: l.slug },
    props: { type: 'local', entry: null, meta: null, clusters: [], local: l, cross: null, hood: null },
  }));

  const crossPaths = crossPages.map(c => ({
    params: { slug: c.slug },
    props: { type: 'cross', entry: null, meta: null, clusters: [], local: null, cross: c, hood: null },
  }));

  const hoodPaths = neighborhoods.map(h => ({
    params: { slug: h.slug },
    props: { type: 'hood', entry: null, meta: null, clusters: [], local: null, cross: null, hood: h },
  }));

  return [...pillarPaths, ...localPaths, ...crossPaths, ...hoodPaths];
}

const { type, entry, meta, clusters, local, cross, hood } = Astro.props;

// Lokasyon sayfaları için benzersizleştirme verileri
let localProfile: any = null;
let localNotes: any = null;
let localServicesOrdered: any[] = services;
let localProblems: any[] = [];
let localNearby: any[] = [];
if (type === 'local' && local) {
  const h = slugHash(local.slug);
  const dkey = districtKeyForSlug(local.slug);
  localProfile = dkey ? districtProfiles[dkey] : null;
  localNotes = dkey ? districtLocalNotes[dkey] : null;
  // Hizmet grid'ini ilçeye göre farklı sırayla göster
  localServicesOrdered = [...services].sort((a, b) => {
    const ha = slugHash(local.slug + a.slug);
    const hb = slugHash(local.slug + b.slug);
    return ha - hb;
  });
  // 28 problemden 6 tanesini hash ile seç (her ilçe farklı)
  const idxSet = new Set<number>();
  for (let i = 0; i < 60 && idxSet.size < 6; i++) {
    idxSet.add(slugHash(local.slug + ':' + i) % commonProblems.length);
  }
  localProblems = [...idxSet].map(i => commonProblems[i]);
  // Yakın bölge cross-link: aynı area'daki hood'lar + diğer pillar ilçeler (deterministik 4)
  const sameHoods = neighborhoods.filter(n => n.parentSlug === local.slug).slice(0, 3);
  const otherLocals = localLandings
    .filter(l => l.slug !== local.slug && l.slug !== 'istanbul-yonetilen-it-hizmetleri' && l.slug !== 'anadolu-yakasi-kurumsal-bilisim')
    .sort((a, b) => slugHash(local.slug + a.slug) - slugHash(local.slug + b.slug))
    .slice(0, 4);
  localNearby = [
    ...sameHoods.map(n => ({ label: `${n.area} IT Destek`, href: `/${n.slug}` })),
    ...otherLocals.map(l => ({ label: l.area + ' IT Destek', href: `/${l.slug}` })),
  ].slice(0, 6);
}

// Cross ve hood için de yerel saha notlarını çöz
let crossNotes: any = null;
if (type === 'cross' && cross) {
  const dkey = districtKeyForSlug(cross.localSlug);
  crossNotes = dkey ? districtLocalNotes[dkey] : null;
}
let hoodNotes: any = null;
if (type === 'hood' && hood) {
  const dkey = districtKeyForSlug(hood.parentSlug);
  hoodNotes = dkey ? districtLocalNotes[dkey] : null;
}

// For pillar pages, render the markdown content
let Content: any = null;
if (type === 'pillar' && entry) {
  const rendered = await render(entry);
  Content = rendered.Content;
}

// Compute layout props based on type
const layoutTitle = type === 'pillar' ? entry.data.title : type === 'cross' ? cross.title : type === 'hood' ? `${hood.area} IT Destek | Bilişim Firması & Bilgi İşlem` : local.title;
const layoutDescription = type === 'pillar' ? entry.data.meta_description : type === 'cross' ? cross.meta : type === 'hood' ? hood.meta : local.meta;
const layoutCanonical = type === 'pillar' ? `/${entry.data.slug || entry.id}` : type === 'cross' ? `/${cross.slug}` : type === 'hood' ? `/${hood.slug}` : `/${local.slug}`;
const layoutOgType = type === 'pillar' ? 'article' : 'website';

const layoutSchema = type === 'pillar'
  ? [
      articleSchema({
        title: entry.data.title,
        description: entry.data.meta_description,
        url: `https://kozyatagibilisim.com/${entry.data.slug || entry.id}`,
      }),
      breadcrumbSchema([
        { label: 'Anasayfa', href: '/' },
        { label: 'Rehberler', href: '/blog' },
        { label: meta.title },
      ]),
    ]
  : type === 'cross'
  ? [
      serviceSchema({ name: cross.title, description: cross.meta, url: `https://kozyatagibilisim.com/${cross.slug}` }),
      localBusinessSchema(cross.area),
      breadcrumbSchema([
        { label: 'Anasayfa', href: '/' },
        { label: `${cross.area} IT Destek`, href: `/${cross.localSlug}` },
        { label: cross.serviceName },
      ]),
      ...(cross.faqs ? [faqSchema(cross.faqs)] : []),
    ]
  : type === 'hood'
  ? [
      localBusinessSchema(hood.area),
      breadcrumbSchema([
        { label: 'Anasayfa', href: '/' },
        { label: `${hood.parentArea} IT Destek`, href: `/${hood.parentSlug}` },
        { label: `${hood.area} IT Destek` },
      ]),
    ]
  : [
      localBusinessSchema(local.area),
      breadcrumbSchema([{ label: 'Anasayfa', href: '/' }, { label: local.title }]),
      ...(local.faqs ? [faqSchema(local.faqs)] : []),
    ];
---

<Layout
  title={layoutTitle}
  description={layoutDescription}
  canonical={layoutCanonical}
  ogType={layoutOgType}
  schema={layoutSchema}
  geoPlacename={type === 'local' ? `İstanbul, ${local.area}` : type === 'cross' ? `İstanbul, ${cross.area}` : type === 'hood' ? `İstanbul, ${hood.area}` : undefined}
>
  <Navbar client:load />
  <WhatsAppButton client:load />
  <StickyContactBar />
  <main class="pb-16 md:pb-0">
    <div class="pt-28 pb-24 bg-slate-950 text-white min-h-screen">
      <div class="container mx-auto px-6 max-w-5xl">

        {type === 'pillar' && (
          <>
            <!-- Breadcrumb -->
            <nav aria-label="Breadcrumb" class="mb-6">
              <ol class="flex items-center flex-wrap gap-1 text-sm text-slate-400">
                <li><a href="/" class="hover:text-cyan-400 transition-colors">Anasayfa</a></li>
                <li class="flex items-center gap-1">
                  <svg class="w-3.5 h-3.5 text-slate-600" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7"/></svg>
                  <a href="/blog" class="hover:text-cyan-400 transition-colors">Rehberler</a>
                </li>
                <li class="flex items-center gap-1">
                  <svg class="w-3.5 h-3.5 text-slate-600" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7"/></svg>
                  <span class="text-slate-300">{meta.title}</span>
                </li>
              </ol>
            </nav>

            <header class="mb-10">
              <div class="inline-block px-3 py-1 rounded-full bg-cyan-900/30 text-cyan-400 text-xs font-semibold mb-4 border border-cyan-800 uppercase tracking-wider">
                Kapsamlı Rehber
              </div>
              <h1 class="text-4xl md:text-6xl font-bold text-white leading-tight mb-4">{entry.data.title}</h1>
              {entry.data.meta_description && (
                <p class="text-lg md:text-xl text-slate-400 max-w-3xl">{entry.data.meta_description}</p>
              )}
            </header>

            <!-- Main content -->
            <article class="prose prose-invert prose-lg max-w-none prose-headings:text-white prose-a:text-cyan-400 prose-strong:text-white prose-code:text-cyan-300 mb-12">
              <Content />
            </article>

            <!-- Inline CTA -->
            <div class="my-10 rounded-3xl bg-gradient-to-br from-blue-900/40 to-cyan-900/30 border border-cyan-700/40 p-8 md:p-10 shadow-xl shadow-cyan-900/10">
              <h3 class="text-2xl md:text-3xl font-bold text-white mb-3">Bu rehberi şirketinizde uygulamak ister misiniz?</h3>
              <p class="text-slate-300 mb-6 leading-relaxed">{meta.title} alanında ücretsiz değerlendirme görüşmesi yapalım. Mevcut altyapınızı analiz edip somut bir yol haritası önerelim.</p>
              <div class="flex flex-col sm:flex-row gap-3">
                <a
                  href="/#contact"
                  class="inline-flex items-center gap-2 bg-blue-600 hover:bg-blue-700 text-white px-6 py-3 rounded-xl font-semibold shadow-lg shadow-blue-500/20 transition-all hover:-translate-y-0.5"
                >
                  Ücretsiz Keşif Talep Et
                  <svg class="w-4.5 h-4.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M17 8l4 4m0 0l-4 4m4-4H3"/></svg>
                </a>
                <span
                  class="inline-flex items-center gap-2 bg-slate-800 text-white px-6 py-3 rounded-xl font-semibold border border-slate-700 whitespace-nowrap cursor-text"
                >
                  <svg class="w-4.5 h-4.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/></svg>
                  0541 636 77 75
                </span>
              </div>
            </div>

            <!-- Related blog posts for this pillar -->
            {clusters.length > 0 && (
              <section class="my-12">
                <h2 class="text-2xl md:text-3xl font-bold text-white mb-6">Bu Konudaki Tüm Rehberler</h2>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-5">
                  {clusters.map((c: any) => (
                    <a
                      href={`/blog/${c.data.slug || c.slug}`}
                      class="group block p-6 bg-slate-900/60 border border-slate-800 rounded-2xl hover:border-cyan-500/40 hover:bg-slate-800/60 transition-all"
                    >
                      <h3 class="text-lg font-semibold text-white group-hover:text-cyan-300 mb-2">{c.data.title}</h3>
                      <p class="text-sm text-slate-400 line-clamp-2 mb-3">{c.data.meta_description}</p>
                      <span class="inline-flex items-center gap-1 text-cyan-400 text-sm font-medium">
                        Devamını oku
                        <svg class="w-3.5 h-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M17 8l4 4m0 0l-4 4m4-4H3"/></svg>
                      </span>
                    </a>
                  ))}
                </div>
              </section>
            )}
          </>
        )}

        {type === 'local' && (
          <>
            <!-- Breadcrumb -->
            <nav aria-label="Breadcrumb" class="mb-6">
              <ol class="flex items-center flex-wrap gap-1 text-sm text-slate-400">
                <li><a href="/" class="hover:text-cyan-400 transition-colors">Anasayfa</a></li>
                <li class="flex items-center gap-1">
                  <svg class="w-3.5 h-3.5 text-slate-600" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7"/></svg>
                  <span class="text-slate-300">{local.title}</span>
                </li>
              </ol>
            </nav>

            <!-- Hero -->
            <header class="mb-8 flex items-start gap-4">
              <div class="bg-cyan-900/30 w-14 h-14 rounded-xl flex items-center justify-center border border-cyan-800 flex-shrink-0">
                <svg class="w-7 h-7 text-cyan-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/><path stroke-linecap="round" stroke-linejoin="round" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/></svg>
              </div>
              <div>
                <h1 class="text-3xl md:text-5xl font-bold text-white leading-tight">{local.h1}</h1>
              </div>
            </header>

            <p class="text-lg text-slate-300 leading-relaxed mb-10">{local.intro}</p>

            {localProfile && (
              <section class="mb-12 p-6 bg-slate-900/30 border border-slate-800/50 rounded-2xl">
                <h2 class="text-2xl font-bold text-white mb-4">{localProfile.area} İş Ekosistemi ve IT Gereksinimleri</h2>
                <p class="text-slate-300 leading-relaxed mb-4">{localProfile.businessLandscape}</p>
                <p class="text-slate-300 leading-relaxed mb-4"><strong class="text-cyan-400">Bölgeye özgü IT zorlukları:</strong> {localProfile.localChallenge}</p>
                <p class="text-slate-400 text-sm leading-relaxed"><strong class="text-slate-300">Tipik müşteri profili:</strong> {localProfile.typicalClient}</p>
                {localProfile.industries && localProfile.industries.length > 0 && (
                  <p class="text-slate-400 text-sm mt-3"><strong class="text-slate-300">Yoğun sektörler:</strong> {localProfile.industries.join(' · ')}</p>
                )}
                {localProfile.proximity && (
                  <p class="text-slate-400 text-sm mt-2"><strong class="text-slate-300">Ulaşım:</strong> {localProfile.proximity}</p>
                )}
              </section>
            )}

            {localNotes && (
              <section class="mb-12">
                <h2 class="text-2xl font-bold text-white mb-2">{local.area} Sahasında Gördüklerimiz</h2>
                <p class="text-slate-400 text-sm mb-6">Yerel forumlarda, IT topluluğunda ve müşteri ziyaretlerimizde {local.area} için tekrar tekrar karşılaştığımız spesifik friksiyon noktaları:</p>
                <div class="space-y-4">
                  {localNotes.fieldNotes.map((n: any) => (
                    <div class="p-5 bg-slate-900/50 border-l-2 border-cyan-700/60 rounded-r-xl">
                      <h3 class="text-cyan-300 font-semibold mb-2">{n.title}</h3>
                      <p class="text-slate-300 text-sm leading-relaxed">{n.body}</p>
                    </div>
                  ))}
                </div>
                {localNotes.technicalEdge && (
                  <div class="mt-5 p-5 bg-emerald-950/20 border border-emerald-800/40 rounded-xl">
                    <h3 class="text-xs font-semibold text-emerald-400 uppercase tracking-wider mb-2">{local.area} Rutinimiz</h3>
                    <p class="text-slate-200 text-sm leading-relaxed">{localNotes.technicalEdge}</p>
                  </div>
                )}
              </section>
            )}

            <!-- Neden Kozyatağı Bilişim -->
            <section class="mb-12">
              <h2 class="text-2xl font-bold text-white mb-5">Neden Kozyatağı Bilişim?</h2>
              <ul class="space-y-3">
                {local.bullets.map((b: string) => (
                  <li class="flex items-start gap-3 text-slate-300">
                    <svg class="w-5 h-5 text-cyan-400 flex-shrink-0 mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
                    <span>{b}</span>
                  </li>
                ))}
              </ul>
            </section>

            <!-- Sunduğumuz Hizmetler -->
            <section class="mb-12">
              <h2 class="text-2xl font-bold text-white mb-5">{local.area}'da Sunduğumuz Hizmetler</h2>
              <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                {localServicesOrdered.map((s) => (
                  <a
                    href={`/hizmetler/${s.slug}`}
                    class="p-5 bg-slate-900/60 border border-slate-800 rounded-xl hover:border-cyan-500/40 transition-all"
                  >
                    <h3 class="text-white font-semibold mb-1">{s.title}</h3>
                    <p class="text-sm text-slate-400">{s.tagline}</p>
                  </a>
                ))}
              </div>
            </section>

            <!-- Detaylı Hizmet Açıklamaları -->
            {local.sections && local.sections.length > 0 && (
              <section class="mb-16">
                <h2 class="text-3xl font-bold text-white mb-8">{local.area} Bölgesinde IT Hizmetlerimiz — Detaylı</h2>
                <div class="space-y-8">
                  {local.sections.map((sec: any) => (
                    <div class="p-6 bg-slate-900/40 border border-slate-800/60 rounded-2xl">
                      <h3 class="text-xl font-bold text-white mb-3">{sec.title}</h3>
                      <p class="text-slate-300 leading-relaxed">{sec.text}</p>
                    </div>
                  ))}
                </div>
              </section>
            )}

            <!-- Sık Sorulan Sorular -->
            {local.faqs && local.faqs.length > 0 && (
              <section class="mb-16">
                <h2 class="text-3xl font-bold text-white mb-8">{local.area} IT Desteği Hakkında Sık Sorulan Sorular</h2>
                <div class="space-y-4">
                  {local.faqs.map((faq: any) => (
                    <div class="p-6 bg-slate-900/40 border border-slate-800/60 rounded-2xl">
                      <h3 class="text-lg font-semibold text-cyan-400 mb-2">{faq.q}</h3>
                      <p class="text-slate-300 leading-relaxed">{faq.a}</p>
                    </div>
                  ))}
                </div>
              </section>
            )}

            <!-- Sık Karşılaşılan IT Sorunları — ilçeye özel seçili 6 (duplicate content azaltıldı) -->
            <section class="mb-16">
              <h2 class="text-3xl font-bold text-white mb-3">{local.area} İşletmelerinde Son Kullanıcı IT Sorunları</h2>
              <p class="text-slate-400 mb-8">{local.area} bölgesinde sahada en sık gördüğümüz son kullanıcı sorunları ve detaylı çözüm rehberleri:</p>
              <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
                {localProblems.map((p) => (
                  <a
                    href={p.blog}
                    class="group p-4 bg-slate-900/60 border border-slate-800 rounded-xl hover:border-cyan-500/40 hover:bg-slate-800/60 transition-all"
                  >
                    <h3 class="text-white font-semibold mb-1 group-hover:text-cyan-300 transition-colors">{p.title}</h3>
                    <p class="text-xs text-slate-500 line-clamp-2">{p.desc}</p>
                  </a>
                ))}
              </div>
            </section>

            {localNearby.length > 0 && (
              <section class="mb-16">
                <h2 class="text-2xl font-bold text-white mb-4">Yakın Bölgelerde IT Desteği</h2>
                <p class="text-slate-400 mb-5">{local.area} yakınındaki diğer hizmet bölgelerimiz:</p>
                <div class="flex flex-wrap gap-3">
                  {localNearby.map((n) => (
                    <a href={n.href} class="px-4 py-2 bg-slate-900/60 border border-slate-800 rounded-lg text-sm text-slate-300 hover:border-cyan-500/40 hover:text-cyan-300 transition-all">
                      {n.label}
                    </a>
                  ))}
                </div>
              </section>
            )}

            <!-- CTA -->
            <div class="my-10 rounded-3xl bg-gradient-to-br from-blue-900/40 to-cyan-900/30 border border-cyan-700/40 p-8 md:p-10 shadow-xl shadow-cyan-900/10">
              <h3 class="text-2xl md:text-3xl font-bold text-white mb-3">{local.area} için ücretsiz keşif görüşmesi</h3>
              <p class="text-slate-300 mb-6 leading-relaxed">Şirketinizin mevcut IT durumunu ücretsiz değerlendirelim. Aynı gün ön görüşme yapabilir, gerekirse saha ziyareti planlayabiliriz.</p>
              <div class="flex flex-col sm:flex-row gap-3">
                <a
                  href="/#contact"
                  class="inline-flex items-center gap-2 bg-blue-600 hover:bg-blue-700 text-white px-6 py-3 rounded-xl font-semibold shadow-lg shadow-blue-500/20 transition-all hover:-translate-y-0.5"
                >
                  Ücretsiz Keşif Talep Et
                  <svg class="w-4.5 h-4.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M17 8l4 4m0 0l-4 4m4-4H3"/></svg>
                </a>
                <span
                  class="inline-flex items-center gap-2 bg-slate-800 text-white px-6 py-3 rounded-xl font-semibold border border-slate-700 whitespace-nowrap cursor-text"
                >
                  <svg class="w-4.5 h-4.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/></svg>
                  0541 636 77 75
                </span>
              </div>
            </div>
          </>
        )}

        {type === 'hood' && (
          <>
            <!-- Breadcrumb -->
            <nav aria-label="Breadcrumb" class="mb-6">
              <ol class="flex items-center flex-wrap gap-1 text-sm text-slate-400">
                <li><a href="/" class="hover:text-cyan-400 transition-colors">Anasayfa</a></li>
                <li class="flex items-center gap-1">
                  <svg class="w-3.5 h-3.5 text-slate-600" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7"/></svg>
                  <a href={`/${hood.parentSlug}`} class="hover:text-cyan-400 transition-colors">{hood.parentArea} IT Destek</a>
                </li>
                <li class="flex items-center gap-1">
                  <svg class="w-3.5 h-3.5 text-slate-600" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7"/></svg>
                  <span class="text-slate-300">{hood.area} IT Destek</span>
                </li>
              </ol>
            </nav>

            <!-- Hero -->
            <header class="mb-8 flex items-start gap-4">
              <div class="bg-cyan-900/30 w-14 h-14 rounded-xl flex items-center justify-center border border-cyan-800 flex-shrink-0">
                <svg class="w-7 h-7 text-cyan-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/><path stroke-linecap="round" stroke-linejoin="round" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/></svg>
              </div>
              <div>
                <h1 class="text-3xl md:text-5xl font-bold text-white leading-tight">{hood.area} IT Destek, Bilişim ve Bilgi İşlem Hizmetleri</h1>
              </div>
            </header>

            <p class="text-lg text-slate-300 leading-relaxed mb-10">{hood.intro}</p>

            <!-- Hizmetler -->
            <section class="mb-12">
              <h2 class="text-2xl font-bold text-white mb-5">{hood.area}'da Sunduğumuz IT Hizmetleri</h2>
              <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                {services.map((s) => (
                  <a
                    href={`/hizmetler/${s.slug}`}
                    class="p-5 bg-slate-900/60 border border-slate-800 rounded-xl hover:border-cyan-500/40 transition-all"
                  >
                    <h3 class="text-white font-semibold mb-1">{s.title}</h3>
                    <p class="text-sm text-slate-400">{s.tagline}</p>
                  </a>
                ))}
              </div>
            </section>

            {hoodNotes && (
              <section class="mb-12">
                <h2 class="text-2xl font-bold text-white mb-2">{hood.parentArea} Çevresinde Sahadan Notlar</h2>
                <p class="text-slate-400 text-sm mb-6">{hood.area}'nın bağlı olduğu {hood.parentArea} bölgesi için tekrar gördüğümüz friksiyon noktaları — {hood.area} ofisiniz için de geçerli:</p>
                <div class="space-y-4">
                  {hoodNotes.fieldNotes.map((n: any) => (
                    <div class="p-5 bg-slate-900/50 border-l-2 border-cyan-700/60 rounded-r-xl">
                      <h3 class="text-cyan-300 font-semibold mb-2">{n.title}</h3>
                      <p class="text-slate-300 text-sm leading-relaxed">{n.body}</p>
                    </div>
                  ))}
                </div>
                {hoodNotes.technicalEdge && (
                  <div class="mt-5 p-5 bg-emerald-950/20 border border-emerald-800/40 rounded-xl">
                    <h3 class="text-xs font-semibold text-emerald-400 uppercase tracking-wider mb-2">{hood.parentArea} Rutinimiz</h3>
                    <p class="text-slate-200 text-sm leading-relaxed">{hoodNotes.technicalEdge}</p>
                  </div>
                )}
              </section>
            )}

            <!-- Neden Biz -->
            <section class="mb-12">
              <h2 class="text-2xl font-bold text-white mb-5">{hood.area}'da Neden Kozyatağı Bilişim?</h2>
              <ul class="space-y-3">
                <li class="flex items-start gap-3 text-slate-300">
                  <svg class="w-5 h-5 text-cyan-400 flex-shrink-0 mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
                  <span>{hood.area} bölgesine aynı gün yerinde müdahale garantisi</span>
                </li>
                <li class="flex items-start gap-3 text-slate-300">
                  <svg class="w-5 h-5 text-cyan-400 flex-shrink-0 mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
                  <span>Ataşehir merkezimizden {hood.parentArea} bölgesine hızlı erişim</span>
                </li>
                <li class="flex items-start gap-3 text-slate-300">
                  <svg class="w-5 h-5 text-cyan-400 flex-shrink-0 mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
                  <span>Aylık sabit ücretli IT outsource ve bakım anlaşmaları</span>
                </li>
                <li class="flex items-start gap-3 text-slate-300">
                  <svg class="w-5 h-5 text-cyan-400 flex-shrink-0 mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
                  <span>7/24 uzaktan destek ve proaktif izleme altyapısı</span>
                </li>
                <li class="flex items-start gap-3 text-slate-300">
                  <svg class="w-5 h-5 text-cyan-400 flex-shrink-0 mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
                  <span>KVKK uyumlu veri koruma ve denetim hizmeti</span>
                </li>
              </ul>
            </section>

            <!-- Üst bölge linki -->
            <section class="mb-12">
              <h2 class="text-2xl font-bold text-white mb-5">{hood.parentArea} Bölgesindeki Diğer Hizmet Alanlarımız</h2>
              <p class="text-slate-400 mb-4">{hood.parentArea} genelindeki tüm IT hizmetlerimiz için ilçe sayfamızı ziyaret edin:</p>
              <a
                href={`/${hood.parentSlug}`}
                class="inline-flex items-center gap-2 px-5 py-3 bg-slate-900/60 border border-cyan-800/40 rounded-xl text-cyan-400 hover:text-cyan-300 hover:border-cyan-500/40 transition-all font-medium"
              >
                {hood.parentArea} IT Destek Sayfasına Git →
              </a>
            </section>

            <!-- CTA -->
            <div class="my-10 rounded-3xl bg-gradient-to-br from-blue-900/40 to-cyan-900/30 border border-cyan-700/40 p-8 md:p-10 shadow-xl shadow-cyan-900/10">
              <h3 class="text-2xl md:text-3xl font-bold text-white mb-3">{hood.area} için ücretsiz keşif görüşmesi</h3>
              <p class="text-slate-300 mb-6 leading-relaxed">{hood.area} bölgesindeki şirketinizin IT altyapısını ücretsiz değerlendirelim. Aynı gün ön görüşme yapabilir, gerekirse yerinde saha ziyareti planlayabiliriz.</p>
              <div class="flex flex-col sm:flex-row gap-3">
                <a
                  href="/#contact"
                  class="inline-flex items-center gap-2 bg-blue-600 hover:bg-blue-700 text-white px-6 py-3 rounded-xl font-semibold shadow-lg shadow-blue-500/20 transition-all hover:-translate-y-0.5"
                >
                  Ücretsiz Keşif Talep Et
                  <svg class="w-4.5 h-4.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M17 8l4 4m0 0l-4 4m4-4H3"/></svg>
                </a>
                <span
                  class="inline-flex items-center gap-2 bg-slate-800 text-white px-6 py-3 rounded-xl font-semibold border border-slate-700 whitespace-nowrap cursor-text"
                >
                  <svg class="w-4.5 h-4.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/></svg>
                  0541 636 77 75
                </span>
              </div>
            </div>
          </>
        )}

        {type === 'cross' && (
          <>
            <!-- Breadcrumb -->
            <nav aria-label="Breadcrumb" class="mb-6">
              <ol class="flex items-center flex-wrap gap-1 text-sm text-slate-400">
                <li><a href="/" class="hover:text-cyan-400 transition-colors">Anasayfa</a></li>
                <li class="flex items-center gap-1">
                  <svg class="w-3.5 h-3.5 text-slate-600" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7"/></svg>
                  <a href={`/${cross.localSlug}`} class="hover:text-cyan-400 transition-colors">{cross.area} IT Destek</a>
                </li>
                <li class="flex items-center gap-1">
                  <svg class="w-3.5 h-3.5 text-slate-600" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7"/></svg>
                  <span class="text-slate-300">{cross.serviceName}</span>
                </li>
              </ol>
            </nav>

            <!-- Hero -->
            <header class="mb-8 flex items-start gap-4">
              <div class="bg-cyan-900/30 w-14 h-14 rounded-xl flex items-center justify-center border border-cyan-800 flex-shrink-0">
                <svg class="w-7 h-7 text-cyan-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.066 2.573c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.573 1.066c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.066-2.573c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"/><path stroke-linecap="round" stroke-linejoin="round" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/></svg>
              </div>
              <div>
                <h1 class="text-3xl md:text-5xl font-bold text-white leading-tight">{cross.h1}</h1>
              </div>
            </header>

            <p class="text-lg text-slate-300 leading-relaxed mb-10">
              {cross.area} bölgesinde <strong class="text-white">{cross.serviceName.toLowerCase()}</strong> hizmeti arıyorsanız doğru yerdesiniz.
              Kozyatağı Bilişim olarak {cross.area} ve çevresindeki işletmelere {cross.serviceName.toLowerCase()} dahil tüm kurumsal IT hizmetlerini yerinde sunuyoruz.
              {cross.districtProfile?.proximity || `Ataşehir merkezimizden koordine edilen saha ekibimiz, aynı gün müdahale garantisiyle yanınızda.`}
            </p>

            <!-- İlçe İş Profili — UNIQUE -->
            {cross.districtProfile && (
              <section class="mb-12 p-6 md:p-8 rounded-2xl bg-slate-900/50 border border-slate-800">
                <h2 class="text-2xl font-bold text-white mb-4">{cross.area}'da İş Ekosistemi</h2>
                <p class="text-slate-300 leading-relaxed mb-4">{cross.districtProfile.businessLandscape}</p>
                <div class="grid md:grid-cols-2 gap-4 mt-5">
                  <div class="p-4 bg-slate-950/40 rounded-xl border border-slate-800/80">
                    <h3 class="text-sm font-semibold text-cyan-400 uppercase tracking-wide mb-2">Tipik Müşteri Profili</h3>
                    <p class="text-slate-300 text-sm leading-relaxed">{cross.districtProfile.typicalClient}</p>
                  </div>
                  <div class="p-4 bg-slate-950/40 rounded-xl border border-slate-800/80">
                    <h3 class="text-sm font-semibold text-cyan-400 uppercase tracking-wide mb-2">Bölgeye Özel IT Zorluğu</h3>
                    <p class="text-slate-300 text-sm leading-relaxed">{cross.districtProfile.localChallenge}</p>
                  </div>
                </div>
                {cross.districtProfile.industries && (
                  <div class="mt-5 flex flex-wrap gap-2">
                    {cross.districtProfile.industries.map((ind: string) => (
                      <span class="px-3 py-1 text-xs rounded-full bg-cyan-900/30 text-cyan-300 border border-cyan-800/60">{ind}</span>
                    ))}
                  </div>
                )}
              </section>
            )}

            <!-- Bölgeye Özel Senaryo + Teknik Yaklaşım — UNIQUE -->
            {cross.serviceAngle && (
              <section class="mb-12 space-y-6">
                <h2 class="text-2xl font-bold text-white">{cross.area}'da {cross.serviceName} — Gerçek Bir Senaryo</h2>
                <div class="p-6 rounded-2xl bg-gradient-to-br from-slate-900/70 to-slate-900/40 border border-slate-800">
                  <h3 class="text-sm font-semibold text-amber-400 uppercase tracking-wide mb-2">Saha Örneği</h3>
                  <p class="text-slate-300 leading-relaxed">{cross.serviceAngle.scenario}</p>
                </div>
                <div class="p-6 rounded-2xl bg-gradient-to-br from-slate-900/70 to-slate-900/40 border border-slate-800">
                  <h3 class="text-sm font-semibold text-cyan-400 uppercase tracking-wide mb-2">Bu Bölgede Neden Kritik?</h3>
                  <p class="text-slate-300 leading-relaxed">{cross.serviceAngle.whyHere}</p>
                </div>
                <div class="p-6 rounded-2xl bg-gradient-to-br from-slate-900/70 to-slate-900/40 border border-slate-800">
                  <h3 class="text-sm font-semibold text-emerald-400 uppercase tracking-wide mb-2">Teknik Yaklaşımımız</h3>
                  <p class="text-slate-300 leading-relaxed">{cross.serviceAngle.technicalAngle}</p>
                </div>
              </section>
            )}

            {crossNotes && (
              <section class="mb-12">
                <h2 class="text-2xl font-bold text-white mb-2">{cross.area} Sahasında Gördüklerimiz</h2>
                <p class="text-slate-400 text-sm mb-6">{cross.area} için IT topluluğunda tekrar eden spesifik friksiyon noktaları — bu ilçede {cross.serviceName.toLowerCase()} tasarımımızı şekillendirir:</p>
                <div class="space-y-4">
                  {crossNotes.fieldNotes.map((n: any) => (
                    <div class="p-5 bg-slate-900/50 border-l-2 border-cyan-700/60 rounded-r-xl">
                      <h3 class="text-cyan-300 font-semibold mb-2">{n.title}</h3>
                      <p class="text-slate-300 text-sm leading-relaxed">{n.body}</p>
                    </div>
                  ))}
                </div>
                {crossNotes.technicalEdge && (
                  <div class="mt-5 p-5 bg-emerald-950/20 border border-emerald-800/40 rounded-xl">
                    <h3 class="text-xs font-semibold text-emerald-400 uppercase tracking-wider mb-2">{cross.area} Rutinimiz</h3>
                    <p class="text-slate-200 text-sm leading-relaxed">{crossNotes.technicalEdge}</p>
                  </div>
                )}
              </section>
            )}

            <!-- Hizmet Özellikleri -->
            {cross.serviceFeatures && cross.serviceFeatures.length > 0 && (
              <section class="mb-12">
                <h2 class="text-2xl font-bold text-white mb-5">{cross.area}'da {cross.serviceName} Kapsamımız</h2>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                  {cross.serviceFeatures.map((f: any) => (
                    <div class="p-5 bg-slate-900/60 border border-slate-800 rounded-xl">
                      <h3 class="text-white font-semibold mb-1">{f.title}</h3>
                      <p class="text-sm text-slate-400">{f.desc}</p>
                    </div>
                  ))}
                </div>
              </section>
            )}

            <!-- Neden Bu Bölgede Biz -->
            <section class="mb-12">
              <h2 class="text-2xl font-bold text-white mb-5">{cross.area}'da Neden Kozyatağı Bilişim?</h2>
              <ul class="space-y-3">
                <li class="flex items-start gap-3 text-slate-300">
                  <svg class="w-5 h-5 text-cyan-400 flex-shrink-0 mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
                  <span>{cross.area} bölgesine aynı gün yerinde müdahale</span>
                </li>
                <li class="flex items-start gap-3 text-slate-300">
                  <svg class="w-5 h-5 text-cyan-400 flex-shrink-0 mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
                  <span>Aylık sabit ücretli {cross.serviceName.toLowerCase()} bakım anlaşması</span>
                </li>
                <li class="flex items-start gap-3 text-slate-300">
                  <svg class="w-5 h-5 text-cyan-400 flex-shrink-0 mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
                  <span>7/24 uzaktan destek ve izleme altyapısı</span>
                </li>
                <li class="flex items-start gap-3 text-slate-300">
                  <svg class="w-5 h-5 text-cyan-400 flex-shrink-0 mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
                  <span>KVKK uyumlu altyapı kurulumu ve denetim</span>
                </li>
                <li class="flex items-start gap-3 text-slate-300">
                  <svg class="w-5 h-5 text-cyan-400 flex-shrink-0 mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
                  <span>Ücretsiz keşif görüşmesi ve detaylı IT analiz raporu</span>
                </li>
              </ul>
            </section>

            <!-- Diğer Hizmetler Bu Bölgede -->
            <section class="mb-12">
              <h2 class="text-2xl font-bold text-white mb-5">{cross.area}'da Diğer IT Hizmetlerimiz</h2>
              <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                {services.filter((s) => s.slug !== cross.serviceSlug).map((s) => (
                  <a
                    href={`/hizmetler/${s.slug}`}
                    class="p-5 bg-slate-900/60 border border-slate-800 rounded-xl hover:border-cyan-500/40 transition-all"
                  >
                    <h3 class="text-white font-semibold mb-1">{s.title}</h3>
                    <p class="text-sm text-slate-400">{s.tagline}</p>
                  </a>
                ))}
              </div>
            </section>

            <!-- FAQs -->
            {cross.faqs && cross.faqs.length > 0 && (
              <section class="mb-16">
                <h2 class="text-3xl font-bold text-white mb-8">{cross.area} {cross.serviceName} — Sıkça Sorulan Sorular</h2>
                <div class="space-y-4">
                  {cross.faqs.map((faq: any) => (
                    <div class="p-6 bg-slate-900/40 border border-slate-800/60 rounded-2xl">
                      <h3 class="text-lg font-semibold text-cyan-400 mb-2">{faq.q}</h3>
                      <p class="text-slate-300 leading-relaxed">{faq.a}</p>
                    </div>
                  ))}
                </div>
              </section>
            )}

            <!-- CTA -->
            <div class="my-10 rounded-3xl bg-gradient-to-br from-blue-900/40 to-cyan-900/30 border border-cyan-700/40 p-8 md:p-10 shadow-xl shadow-cyan-900/10">
              <h3 class="text-2xl md:text-3xl font-bold text-white mb-3">{cross.area}'da {cross.serviceName} için teklif alın</h3>
              <p class="text-slate-300 mb-6 leading-relaxed">Ücretsiz keşif görüşmesinde {cross.area} bölgesindeki şirketinizin {cross.serviceName.toLowerCase()} ihtiyaçlarını birlikte değerlendirelim.</p>
              <div class="flex flex-col sm:flex-row gap-3">
                <a
                  href="/#contact"
                  class="inline-flex items-center gap-2 bg-blue-600 hover:bg-blue-700 text-white px-6 py-3 rounded-xl font-semibold shadow-lg shadow-blue-500/20 transition-all hover:-translate-y-0.5"
                >
                  Ücretsiz Keşif Talep Et
                  <svg class="w-4.5 h-4.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M17 8l4 4m0 0l-4 4m4-4H3"/></svg>
                </a>
                <span
                  class="inline-flex items-center gap-2 bg-slate-800 text-white px-6 py-3 rounded-xl font-semibold border border-slate-700 whitespace-nowrap cursor-text"
                >
                  <svg class="w-4.5 h-4.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/></svg>
                  0541 636 77 75
                </span>
              </div>
            </div>
          </>
        )}

      </div>
    </div>
  </main>
  <Footer />
  <ConsentNotice client:idle />
</Layout>
